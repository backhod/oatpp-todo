{
  "local": {
    "host": "0.0.0.0",
    "port": 8000,
    "swaggerHost": "localhost:8000",
    "dbConnectionString": "postgresql://postgres:example@localhost:5432"
  },
  "docker": {
    "host": "0.0.0.0",
    "port": 8000,
    "swaggerHost": "localhost:8000",
    "dbConnectionString": "postgresql://postgres:example@db:5432"
  }
}